export const PROFILE = {
  height: 176,
  age: 23,
  activity: 1.45,
  startWeight: 102,
  goalWeight: 80,
} as const;

/**
 * Фиксированные цели на дефиците (21.07.2026, по запросу владельца).
 *
 * Раньше нормы считались из веса по Миффлину-Сан Жеору и уезжали вниз вслед
 * за похудением. Теперь диапазоны заданы явно и не пересчитываются.
 *
 * Белок — главный приоритет: на дефиците он удерживает мышцы, поэтому
 * 160 г — это нижняя граница, а не цель «примерно».
 */
export const NUTRITION_GOALS = {
  kcalMin: 2200,
  kcalMax: 2400,
  proteinMin: 160,
  proteinMax: 180,
} as const;

/** Целевой темп снижения веса, кг в неделю (быстрее — теряются мышцы) */
export const WEIGHT_PACE = { fastest: -0.8, slowest: -0.5 } as const;

/** Шаг коррекции калорий, если темп две недели подряд вне коридора */
export const KCAL_STEP = { min: 150, max: 200 } as const;
